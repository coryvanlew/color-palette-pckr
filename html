<!DOCTYPE html>
<html>
<head>
  <title>Color Palette Generator</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
    }
    #color-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
    }
    .color-box {
      width: 100px;
      height: 100px;
      margin: 10px;
      display: inline-block;
      cursor: pointer;
    }
    .color-code {
      font-size: 14px;
    }
  </style>
</head>
<body>
  <h1>Color Palette Generator</h1>
  <div id="color-container"></div>
  <button id="generate-palette">Generate Palette</button>
  <script>
    let mayanColorBank = [
  // Reds
  '#FF0000', '#FF6347', '#D43E68',
  // Oranges
  '#FFA500', '#FF8C00', '#FF4500',
  // Yellows
  '#FFFF00', '#FFD700', '#FFA07A',
  // Greens
  '#008000', '#228B22', '#00FF00',
  // Teals
  '#008080', '#00CED1', '#20B2AA',
  // Blues
  '#0000FF', '#1E90FF', '#4169E1',
  // Purples
  '#800080', '#9370DB', '#8A2BE2',
  // Browns
  '#A0522D', '#D2691E', '#8B4513',
  // Grays
  '#808080', '#A9A9A9', '#D3D3D3',
  // Whites
  '#FFFFFF', '#F0FFFF', '#FFFAFA',
  // Blacks
  '#000000', '#333333', '#666666'
];

    ];

    function displayColors() {
      const colorContainer = document.getElementById('color-container');
      colorContainer.innerHTML = '';

      mayanColorBank.forEach(color => {
        const colorBox = document.createElement('div');
        colorBox.className = 'color-box';
        colorBox.style.backgroundColor = color;
        colorBox.addEventListener('click', () => {
          removeColor(color);
          displayColors();
        });

        const colorCode = document.createElement('div');
        colorCode.className = 'color-code';
        colorCode.textContent = color;
        
        colorBox.appendChild(colorCode);
        colorContainer.appendChild(colorBox);
      });
    }

    function addColor() {
      const colorInput = prompt('Enter a color code (e.g., #RRGGBB):');
      if (colorInput) {
        mayanColorBank.push(colorInput);
        displayColors();
      }
    }

    function removeColor(color) {
      const index = mayanColorBank.indexOf(color);
      if (index !== -1) {
        mayanColorBank.splice(index, 1);
      }
    }

    document.getElementById('generate-palette').addEventListener('click', () => {
      const colorPalettes = [];

      for (let i = 0; i < 16; i++) {
        const randomIndex = Math.floor(Math.random() * mayanColorBank.length);
        colorPalettes.push(mayanColorBank[randomIndex]);
      }

      alert('Generated Palette:\n\n' + colorPalettes.join('\n'));
    });

    displayColors();
  </script>
</body>
</html>
